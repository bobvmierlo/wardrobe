name: Build and publish container

# Runs when a GitHub Release is published. In one job it stamps the release
# version into backend/app/_version.py (the single source of truth), commits it
# back to main with [skip ci], then builds the Docker image from that working
# tree and pushes it to the GitHub Container Registry (ghcr.io/<owner>/<repo>).
#
# Triggers on `release` only (not the tag push), so a release builds exactly
# once. Plain pushes to main do not produce a container.

on:
  release:
    types: [published]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Stamp version into _version.py
        id: stamp
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION=$(python3 scripts/bump_version.py "${{ github.event.release.tag_name }}")
          else
            VERSION=$(python3 scripts/bump_version.py --get)
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Version: $VERSION"

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add backend/app/_version.py
          if git diff --cached --quiet; then
            echo "No version change to commit"
          else
            git commit -m "chore: release v${{ steps.stamp.outputs.version }} [skip ci]"
            git push origin HEAD:main
          fi

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive image metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          # Tags:
          #  - <version>          the release version (e.g. "0.1.0")
          #  - <major>.<minor>, <major>   when the version is full semver
          #  - latest             newest published release
          #  - <short-sha>        precise pinning
          tags: |
            type=raw,value=${{ steps.stamp.outputs.version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.stamp.outputs.version }}
            type=semver,pattern={{major}},value=${{ steps.stamp.outputs.version }}
            type=sha,format=short
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
